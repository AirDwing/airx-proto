syntax = "proto3";

// 位置传感器
message GNSS {
  uint32 satellite = 1; // 卫星数
  double latitude = 2; // 纬度
  double longitude = 3;  // 经度
  double amsl = 4; // 海拔高度,单位:m
  double ato = 5; // 相对地面高度,单位:m
}